# this is an example bash file for running a scenario
# basically, you only need a jar-file, specify the class conaining your main method and a MATSim config file
java -mx8g -cp gapV2.jar playground.dhosse.gap.run.GAPServerRunner config.xml
