package playground.dhosse.utils.io.dbdata;


public class Gtfs {
/*
	public static void main(String args[]){
		
		Scenario scenario = ScenarioUtils.createScenario(ConfigUtils.createConfig());
		
		new MatsimNetworkReader(scenario).readFile("/home/dhosse/inspire-railNetwork.xml");
		
		CoordinateTransformation ct = TransformationFactory.getCoordinateTransformation(
				"EPSG:32632", TransformationFactory.WGS84);
		
		for(Node node : scenario.getNetwork().getNodes().values()){
			
			((NodeImpl)node).setCoord(ct.transform(node.getCoord()));
			
		}
		
		String filebase = "/home/dhosse/Downloads/db-fv-gtfs-2016.0.0/";
		
		MyGTFS2MATSimTransitSchedule g2m = new MyGTFS2MATSimTransitSchedule(
				new File[]{new File(filebase)},
				new String[]{"rail"}, 
				scenario.getNetwork(),
				new String[]{"0"}, 
				"EPSG:32632");
		
		new TransitScheduleWriter(g2m.getTransitSchedule()).writeFile(filebase + "schedule.xml.gz");
		
	}
*/	
}
